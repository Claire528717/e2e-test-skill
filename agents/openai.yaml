interface:
  display_name: "E2E Test Skill"
  short_description: "为 PRD 和页面生成带覆盖审计的端到端验收测试计划（按用户语言输出）"
  default_prompt: "Use $e2e-test-skill to turn this PRD and UI into a reviewable E2E acceptance test plan in the user's language, with a concise decision digest, PRD coverage audit, realistic UI paths, mock data needs, independent verification, and a clear run file layout. Stop for my review before execution."